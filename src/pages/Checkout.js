import React from 'react'
import NavBar from '../components/NavBar';
// on checkout branch
const CheckOutPage = () => (
    <div>
       <NavBar 
            cart={[1,2]}
        />
    </div>
);

export default CheckOutPage;